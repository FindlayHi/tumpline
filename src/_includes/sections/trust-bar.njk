<div class="border-t border-b border-line bg-band">
  <div class="max-w-[1180px] mx-auto px-8 py-[18px] flex flex-wrap justify-center items-center gap-x-[34px] gap-y-[14px] text-sm font-medium text-muted">
    {% for item in site.trust %}
      <span class="inline-flex items-center gap-[9px]">
        <span class="accent-dot"></span>{{ item }}
      </span>
    {% endfor %}
  </div>
</div>
